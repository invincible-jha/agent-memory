{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "agent-memory.serviceAccountName" . }}
  labels:
    {{- include "agent-memory.labels" . | nindent 4 }}
automountServiceAccountToken: false
{{- end }}
