apiVersion: v1
kind: Service
metadata:
  name: {{ include "agent-memory.fullname" . }}
  labels:
    {{- include "agent-memory.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agent-memory.selectorLabels" . | nindent 4 }}
